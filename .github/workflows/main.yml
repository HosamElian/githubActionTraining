name: First Workflow

on:
  push: 
    branches:
      - main
jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a Welcome Message
        run: echo "Welcome to our first Github Action Workflow"
        
      - name: Checkout repository
        uses: actions/checkout@v5.0.0
      
      - name: List repositories files
        run: ls -la
